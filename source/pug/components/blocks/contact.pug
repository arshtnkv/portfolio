mixin contact()
  section#contact.section.contact&attributes(attributes)
    .container
      h2.contact__title.title.title--h2.title--center.title--effect(data-item="contact") Contact me
      +contact-form()
